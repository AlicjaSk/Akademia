@model IEnumerable<BookingSystem.Models.Room>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfRoom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FloorNr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeOfRoom.Information)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfRoom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FloorNr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeOfRoom.Information)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDRoom }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDRoom }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDRoom })
        </td>
    </tr>
}

</table>
